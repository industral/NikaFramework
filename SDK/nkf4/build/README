TO INSTALL:

NodeJS >= v0.10

$ sudo gem install sass
$ sudo npm install

TO BUILD:

$ cd build
$ node build.js

OPTIONS:
--js-source-map JavaScript Source Maps version 3
--js-optimization=[simple|advanced]
--sass-debug for FireSass
